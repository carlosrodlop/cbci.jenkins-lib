It is a CI pipeline to collect jstack or heap data for Troubleshooting CPU or Memory respectively

Parameters

- mode: Different types of collecting data. (1) for cpu and (2) for memory.

Example:

	performance(1) for cpu
	performance(2) for memory
